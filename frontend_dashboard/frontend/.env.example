# Optional: public Flask API base URL (no trailing slash).
# Local default if unset: http://127.0.0.1:5001
# Vercel: set VITE_API_BASE in Project → Settings → Environment Variables
VITE_API_BASE=http://127.0.0.1:5001
